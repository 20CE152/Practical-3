print("Enter The no of participants")
n=int(input())  # taking input for n
print("Enter the participants score")
arr=map(int,input().split())  # input for runner-ups
print(sorted(list(set(arr)))[-2])  # sorting the list
